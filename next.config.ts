import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Content is loaded from the filesystem at build/request time.
  // Nothing special needed here yet, but this is where you'd add
  // image domains, redirects, etc. as the library grows.
};

export default nextConfig;
